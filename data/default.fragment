#version 400
in vec2 UV;
in vec3 fragmentColor;
in vec3 normal;
out vec3 color;
uniform sampler2D myTextureSampler;
void main () {
  color = texture( myTextureSampler, UV ).rgb * dot(normalize(normal),vec3(1,0,0));
//  color = vec3(1,1,0) * dot(normalize(normal),vec3(1,0,0));
}

